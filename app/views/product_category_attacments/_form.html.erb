<%= form_for(@product_category_attacment) do |f| %>
  <% if @product_category_attacment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_category_attacment.errors.count, "error") %> prohibited this product_category_attacment from being saved:</h2>

      <ul>
      <% @product_category_attacment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
