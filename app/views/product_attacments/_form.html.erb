<%= form_for(@product_attacment) do |f| %>
  <% if @product_attacment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_attacment.errors.count, "error") %> prohibited this product_attacment from being saved:</h2>

      <ul>
      <% @product_attacment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
