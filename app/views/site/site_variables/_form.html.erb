<%= form_for(@site_variable) do |f| %>
  <% if @site_variable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site_variable.errors.count, "error") %> prohibited this site_variable from being saved:</h2>

      <ul>
      <% @site_variable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
