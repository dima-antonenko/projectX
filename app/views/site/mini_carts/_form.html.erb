<%= form_for(@mini_cart) do |f| %>
  <% if @mini_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mini_cart.errors.count, "error") %> prohibited this mini_cart from being saved:</h2>

      <ul>
      <% @mini_cart.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
